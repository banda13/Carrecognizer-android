<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <SwitchPreference
        android:defaultValue="true"
        android:key="clever_pre_recognition"
        android:summary="Capture button will bounce, when it sees a car. It will help to take picture cars from a good angle, and it increase the accuracy significantly"
        android:title="Clever pre-recognition" />

    <SwitchPreference
        android:defaultValue="true"
        android:key="enable_clever_cache"
        android:summary="It helps to improve blurry, bad pictures, and increase accuracy"
        android:title="Increase image quality" />

    <ListPreference
        android:defaultValue="3"
        android:entries="@array/compress_rates_entries"
        android:entryValues="@array/compress_rates_values"
        android:key="compress_rate"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:summary="Save you a lot of mobile data while compressing image quality. (Not decrease accuracy, and not effects saving pictures into gallery)"
        android:title="Compress picture rate" />


</PreferenceScreen>